+++
title = "waves & particles"
description = "••• •• ••• •• •••"
date = "2026-04-08T00:00:00Z"
url = "https://taylor.town/waves"
author = "Taylor Troesh"
text = ""
lastupdated = "2026-04-20T13:20:40.111233747Z"
seen = true
+++

body { background: #000; color: #fff; overflow: hidden; } body\>header { margin: 0 auto 1rem; } main { max-width: none; margin: 0; padding: 0; } main \> \* { max-width: none; margin: 0; padding: 0; } body::before, body \> main::before, body \> main::after { display: none; } canvas { display: block; touch-action: none; width: 100vw; height: 100vh; } #credit { position: fixed; bottom: 12px; left: 0; right: 0; text-align: right; font: 11px/1 sans-serif; padding-right: 16px; color: rgba(255,255,255,0.3); pointer-events: none; } #credit a { color: rgba(255,255,255,0.4); pointer-events: auto; text-decoration: none; } #credit a:hover { color: rgba(255,255,255,0.7); }

inspired by [zach lieberman](https://x.com/zachlieberman/status/1767731728917876844)

 const canvas = document.getElementById('c'); const ctx = canvas.getContext('2d'); let W, H, dots, curX, curY, velX, velY; const SPACING = 12, DOT\_R = 1.2; const BASE\_AMP = 30, BASE\_FREQ = 0.05; const WAVE\_SPEED = 0.5; const STIFFNESS = 200, DAMPING = 14; // State: "idle" | "waving" | "pressed" | "returning" let state = 'idle'; let ptrX = 0, ptrY = 0; let wavePhase = 0, easeIn = 0; function rebuild() { const dpr = window.devicePixelRatio || 1; W = window.innerWidth; H = window.innerHeight; canvas.width = W \* dpr; canvas.height = H \* dpr; canvas.style.width = W + 'px'; canvas.style.height = H + 'px'; ctx.setTransform(dpr, 0, 0, dpr, 0, 0); const cols = Math.ceil(W / SPACING) + 2; const rows = Math.ceil(H / SPACING) + 2; dots = []; for (let r = 0; r \< rows; r++) for (let c = 0; c \< cols; c++) dots.push(c \* SPACING, r \* SPACING); curX = new Float32Array(dots.length / 2); curY = new Float32Array(dots.length / 2); velX = new Float32Array(dots.length / 2); velY = new Float32Array(dots.length / 2); } rebuild(); // Avoid spurious mouseleave from canvas resize: rebuild via rAF let resizeId = 0; window.addEventListener('resize', () =\> { cancelAnimationFrame(resizeId); resizeId = requestAnimationFrame(rebuild); }); function hitTest(x, y) { return x \>= 0 && x \<= W && y \>= 0 && y \<= H; } function getParams(mx, my) { const dx = mx - W / 2, dy = my - H / 2; const angle = Math.atan2(dy, dx); const dist = Math.sqrt(dx \* dx + dy \* dy); const maxDist = Math.sqrt((W / 2) \*\* 2 + (H / 2) \*\* 2); const blend = Math.min(1, dist / (maxDist \* 0.3)); return { freq: BASE\_FREQ \* (0.7 + 0.3 \* Math.sin(angle \* 2) \* blend), amp: BASE\_AMP \* (0.7 + 0.3 \* Math.sin(angle \* 3) \* blend), sharp: 5 + 5 \* (0.5 + 0.5 \* Math.sin(angle \* 5) \* blend), }; } function computeDisp(dotX, dotY, mx, my) { const centers = [[mx, my], [W - mx, H - my]]; const { amp, freq, sharp } = getParams(mx, my); let gx = 0, gy = 0; for (let idx = 0; idx \< 2; idx++) { const dx = dotX - centers[idx][0], dy = dotY - centers[idx][1]; const dist = Math.sqrt(dx \* dx + dy \* dy); if (dist \< 1) continue; const disp = Math.tanh(sharp \* Math.sin(dist \* freq - wavePhase + idx \* Math.PI \* 0.5)); gx += disp \* amp \* (dx / dist); gy += disp \* amp \* (dy / dist); } return [gx, gy]; } function activate(x, y) { ptrX = x; ptrY = y; if (state !== 'waving') { wavePhase = 0; easeIn = 0; } state = 'waving'; } function deactivate() { if (state === 'waving') state = 'returning'; } // Pointer events (mouse) // Use pointermove on window so we never miss the pointer being over the canvas window.addEventListener('pointermove', e =\> { if (e.pointerType === 'touch') return; ptrX = e.clientX; ptrY = e.clientY; if (state === 'idle' || state === 'returning') { if (hitTest(ptrX, ptrY)) activate(ptrX, ptrY); } }); canvas.addEventListener('pointerleave', e =\> { if (e.pointerType === 'touch') return; if (state === 'waving') deactivate(); if (state === 'pressed') state = 'returning'; }); canvas.addEventListener('pointerdown', e =\> { if (e.pointerType === 'touch') { activate(e.clientX, e.clientY); return; } if (state === 'waving') state = 'pressed'; }); window.addEventListener('pointerup', e =\> { if (e.pointerType === 'touch') { deactivate(); return; } if (state === 'pressed') { if (hitTest(e.clientX, e.clientY)) { activate(e.clientX, e.clientY); } else state = 'returning'; } }); canvas.addEventListener('pointercancel', () =\> deactivate()); // Touch move canvas.addEventListener('touchmove', e =\> { e.preventDefault(); ptrX = e.touches[0].clientX; ptrY = e.touches[0].clientY; }, { passive: false }); canvas.addEventListener('touchstart', e =\> e.preventDefault(), { passive: false }); let lastTime = performance.now(); function draw(now) { const dt = (now - lastTime) / 1000; lastTime = now; ctx.fillStyle = '#000'; ctx.fillRect(0, 0, W, H); ctx.fillStyle = '#fff'; if (state === 'waving') { wavePhase += WAVE\_SPEED \* dt; easeIn = Math.min(1, easeIn + dt \* 6); } if (state === 'returning') { let settled = true; for (let j = 0; j \< curX.length; j++) { if (Math.abs(curX[j]) \> 0.01 || Math.abs(curY[j]) \> 0.01 || Math.abs(velX[j]) \> 0.01 || Math.abs(velY[j]) \> 0.01) { settled = false; break; } } if (settled) { state = 'idle'; curX.fill(0); curY.fill(0); velX.fill(0); velY.fill(0); } } const ef = easeIn \* easeIn; const clampDt = Math.min(dt, 0.033); for (let i = 0; i \< dots.length; i += 2) { const j = i \>\> 1; let x = dots[i], y = dots[i + 1]; if (state === 'waving') { const [gx, gy] = computeDisp(x, y, ptrX, ptrY); const tx = gx \* ef, ty = gy \* ef; const ax = STIFFNESS \* (tx - curX[j]) - DAMPING \* velX[j]; const ay = STIFFNESS \* (ty - curY[j]) - DAMPING \* velY[j]; velX[j] += ax \* clampDt; velY[j] += ay \* clampDt; curX[j] += velX[j] \* clampDt; curY[j] += velY[j] \* clampDt; x += curX[j]; y += curY[j]; } else if (state === 'returning' || state === 'pressed') { const ax = STIFFNESS \* (0 - curX[j]) - DAMPING \* velX[j]; const ay = STIFFNESS \* (0 - curY[j]) - DAMPING \* velY[j]; velX[j] += ax \* clampDt; velY[j] += ay \* clampDt; curX[j] += velX[j] \* clampDt; curY[j] += velY[j] \* clampDt; x += curX[j]; y += curY[j]; } ctx.beginPath(); ctx.arc(x, y, DOT\_R, 0, Math.PI \* 2); ctx.fill(); } requestAnimationFrame(draw); } requestAnimationFrame(draw);